{
  "name": "lib",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc && vite build",
    "minify": "terser -c -m --module dist/the-supersonic-plugin-for-scroll-based-animation.js > dist/the-supersonic-plugin-for-scroll-based-animation.min.js",
    "test:unit": "vitest run",
    "test:unit:ui": "vitest --api.host 0.0.0.0 --api.port 8000 --ui"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "@vitest/ui": "^1.5.0",
    "terser": "^5.30.3",
    "typescript": "^5.2.2",
    "vite": "^5.2.0",
    "vite-plugin-dts": "^3.8.1",
    "vitest": "^1.5.0"
  }
}
